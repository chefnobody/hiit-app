<%= form_for(athelete) do |f| %>
  <% if athelete.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(athelete.errors.count, "error") %> prohibited this athelete from being saved:</h2>

      <ul>
      <% athelete.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :first_name %>
    <%= f.text_field :first_name %>
  </div>

  <div class="field">
    <%= f.label :last_name %>
    <%= f.text_field :last_name %>
  </div>

  <div class="field">
    <%= f.label :age %>
    <%= f.number_field :age %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
